<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mechoori.web.repository.RestaurantRepository">
	<select id="findById" resultType="Restaurant">
		select * 
		from restaurant
		where id = #{restaurantId}
	</select>

	<select id="findAll" resultType="Restaurant">
		select *
		from restaurant
		<trim prefix="where" prefixOverrides="AND|OR">
			<if test="categoryId != null">
				category_id = #{categoryId};
			</if>
			<if test="query != null">
				name like '%${query}%'
			</if>
		</trim>
		<if test="page != null">
			limit #{page}, #{size}
		</if>
	</select>

	<!-- TODO 새로운 쿼리문 작성 -->
	<select id="findAllRestaurantView" resultType="RestaurantView">
		
	</select>

	<insert id="add" parameterType = "Restaurant">
		insert into restaurant(category_id, name, img, intro, address, operating_time, contact_number)
		values(#{categoryId}, #{name}, #{img}, #{intro}, #{address}, #{operatingTime}, #{contactNumber})
	</insert>

	<select id="getRanking" resultType="RestaurantView">
		SELECT *
		from restaurant_card
		where category_id = ${categoryId}
		ORDER BY avg_rated_price DESC;
	</select>
</mapper>